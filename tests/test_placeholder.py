"""
Placeholder test so pytest has something to collect. pytest exits with
code 5 when zero tests exist at all, which is what happens with no test
suite merged yet. This gets replaced once tests/test_cnic.py (the
Integration Engineer's Week 2 deliverable) lands as its own PR.
"""


def test_placeholder():
    assert True